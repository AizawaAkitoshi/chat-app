  <div class = "side_bar" >
    <div class="side_top">
      <h2 <%= link_to current_user.name, edit_user_path(current_user) %></h2>
      <%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete }, class: 'btn'%>
      <div class="make_chat_btn">
        <%= link_to 'チャットを作成する', new_room_path %>
      </div>  
      <%# <span><%= current_user.nickname %>
      <%# <%= link_to 'ボタンテキスト', new_page_path, class: 'button-class' %>
    </div>
    <div class="side_list">
    <%# <%= link_to current_user.name, edit_user_path(current_user) %> 
    <% current_user.rooms.each do |room| %>
      <div class="room">
      <div class="room-name">
      <%= link_to room.name, room_messages_path(room) %>
      </div>
    </div>
    <% end %>
    </div>
  </div>